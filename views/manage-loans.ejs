<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center">
            <h1><%= title %></h1>
            <a href="/books" class="btn btn-secondary">Back to Library</a>
        </div>
        <hr>
        <table class="table table-striped mt-3">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Borrowed By</th>
                    <th>Due Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (loans.length > 0) { %>
                    <% loans.forEach(loan => { %>
                        <tr>
                            <td><%= loan.title %></td>
                            <td><%= loan.username %></td>
                            <td><%= new Date(loan.due_date).toLocaleDateString() %></td>
                            <td>
                                <form action="/loans/return/<%= loan.loan_id %>" method="POST">
                                    <button type="submit" class="btn btn-success btn-sm">Mark as Returned</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4" class="text-center">There are no books currently on loan.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>